{
  "name": "backpack-planner",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n client,server -c cyan,green \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
